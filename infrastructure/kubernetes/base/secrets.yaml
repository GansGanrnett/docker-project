apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: default
type: Opaque
stringData:
  # ЗАПОЛНИТЬ перед применением: kubectl apply -k base/ или редактировать
  # значениями из .env (НЕ КОММИТИТЬ реальные значения; используйте
  # sealed-secrets / SOPS / external-secrets в проде).
  auth-db-user: "auth_user"
  auth-db-password: "CHANGE_ME"
  catalog-db-user: "catalog_user"
  catalog-db-password: "CHANGE_ME"
  mongo-root-user: "admin"
  mongo-root-password: "CHANGE_ME"
  rabbitmq-user: "admin"
  rabbitmq-password: "CHANGE_ME"
  redis-url: "redis://:CHANGE_ME@cart-redis-service:6379"
  mongo-url: "mongodb://admin:CHANGE_ME@order-mongodb-service:27017/?authSource=admin"
  rabbitmq-url: "amqp://admin:CHANGE_ME@message-rabbitmq-service:5672"
  spring-datasource-password: "CHANGE_ME"
  auth-admin-password: "CHANGE_ME"