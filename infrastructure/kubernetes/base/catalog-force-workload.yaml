apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: catalog-service
  template:
    metadata:
      labels:
        app: catalog-service
    spec:
      containers:
        - name: catalog-service
          image: catalog-service:local
          imagePullPolicy: Never
          ports:
            - containerPort: 8082
          env:
            - name: RABBITMQ_URL
              value: "amqp://admin:ProdRabbitBrokerPass2026Secure99@infrastructure-databases:5672"
---
apiVersion: v1
kind: Service
metadata:
  name: catalog-service
spec:
  ports:
    - port: 80
      targetPort: 8082
  selector:
    app: catalog-service
